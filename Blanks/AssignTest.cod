class "";

inclass

inobject

--------------------------------------------------------------------------------
--{ Обработчики событий бланка

  proc run(Sender :Button);

    func test(test_list: List): List;

      result = List.create;
      result.assign(test_list);
    end;

    var test_list, other_list: List;

    test_list = List.create;
    test_list.add(1);
    test_list.add(2);

    trace(test_list);

    other_list = test(test_list);
    trace(other_list);
  end;

  proc run_other(Sender :Button);

    func test(test_dict: Dict): Dict;
      
      result = Dict.create;
      result.assign(test_dict);
    end;

    var test_dict, other_dict: Dict;

    test_dict = Dict.combine(["a", "b"], [1, 2]);
    trace(test_dict);

    other_dict = test(test_dict);
    trace(other_dict);
  end;

--}

end