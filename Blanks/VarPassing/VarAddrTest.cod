class "";
import classes Functions.Variable;

inclass

inobject

--------------------------------------------------------------------------------
--{ Обработчики событий бланка

  proc run(Sender :Button);

    var test_array: variant[];
    var test_str: string;
    var addr: integer;

    test_array = [1, 2, 3, 4, 5];
    test_str = "abcde";

--    работает нормально

--    addr = varAddr(test_str);
--
--    trace(addr);
--    trace(System.readMemory(addr, System.varString));
--    trace(System.readMemory(addr, System.varString, 3));


--    не работает
--    в первом случае выводятся иероглифы,
--    во втором адрес и часть адреса

--    addr = varAddr("test_str");
--
--    trace(addr);
--    trace(System.readMemory(addr, System.varString));
--    trace(System.readMemory(addr, System.varString, 3));
--
--    addr = var_address(test_str);
--
--    trace(addr);
--    trace(System.readMemory(addr, System.varString));
--    trace(System.readMemory(addr, System.varString, 3));

--    получился какой-то бред, кажется функция работает только со строками
  end;

--}

end