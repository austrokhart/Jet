class "";

inclass

inobject

--------------------------------------------------------------------------------
--{ Обработчики событий бланка

  proc run(Sender :Button);

    SmartConsole.new.debug_only(true).clear.log_multi(UnitTesting.Runner.run([Tests.Functions.DateSuite]).to_text);
  end;

--}

end