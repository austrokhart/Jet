class "";
import classes Packages.SmartConsole;

inclass

inobject

--------------------------------------------------------------------------------
--{ Обработчики событий бланка

  proc run(Sender :Button);

    SmartConsole.new(true).clear.log_multi(UnitTesting.Runner.run([Tests.Functions.ArraySuite, Tests.Functions.MDArraySuite]).to_text);
  end;

--}

end