class "";
import classes Packages.SmartConsole;

inclass

inobject

--------------------------------------------------------------------------------
--{ Обработчики событий бланка

  proc run(Sender :Button);

    SmartConsole.new(true).clear.log_multi(UnitTesting.Runner.run([Tests.DictSuite]).to_text);
  end;

--}

end